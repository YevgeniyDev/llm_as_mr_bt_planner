# Native baseline records

These records are intentionally separate from complete immutable experiment
snapshots. They preserve external baseline outcomes that can be run without an LLM
API key. The full paper snapshot is created only after every matrix condition has
completed.
